package try_in_home.lambda$stream;

import java.util.Arrays;
import java.util.Comparator;
import java.util.List;
import java.util.stream.Collectors;
/// task1
//List<Integer> numbers = Arrays.asList(5, 2, 8, 1, 9, 3);
//Lambda ilə siyahını azalan sıra ilə düz və çap et

/// task2
//List<String> names = Arrays.asList("ali", "aydan", "nərgiz", "rəşad", "leyla");
//Adları uzunluqlarına görə artan sıra ilə düz (ən qısa → ən uzun);

/// task3
//List<Integer> numbers = Arrays.asList(1, 6, 3, 8, 4, 7, 2);
//5-dən böyük ədədləri filterlə, kvadratını al, nəticəni List kimi topla
public class task1 {
    public static void main(String[] args) {
//       var numbers= Arrays.asList(5,2,8,1,9,3);
//        numbers.stream().sorted(Comparator.reverseOrder())
//                .forEach(System.out::println);

//        var names = Arrays.asList("ali", "aydan", "nərgiz", "rəşad", "leyla");
//        names.stream().sorted(Comparator.comparingInt(String::length)).forEach(System.out::println);
//
//        var numbers = Arrays.asList(1, 6, 3, 8, 4, 7, 2);
//        var result = numbers.stream().filter(number -> number > 5).map(n -> n * n).toList();
//        System.out.println(result);

    
    }


}
